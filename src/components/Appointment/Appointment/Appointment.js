import React from 'react';
import SearchBox from '../SearchBox/SearchBox';

const Appointment = () => {
  return (
    <div>
      <SearchBox></SearchBox>
    </div>
  );
};

export default Appointment;